services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: whatsapp-propensity-api
    env_file:
      - .env
    ports:
      - "8000:8000"
    restart: unless-stopped

